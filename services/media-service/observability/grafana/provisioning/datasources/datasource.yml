# Grafana 数据源自动预置：指向同 compose 网络内的 prometheus 服务
# uid 固定为 "prometheus"，仪表盘 JSON 以该 uid 引用数据源，保证开箱即用
apiVersion: 1

datasources:
  - name: Prometheus
    uid: prometheus
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true
    editable: false
